import { FilterRolePipe } from './filter-role.pipe';

describe('FilterRolePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRolePipe();
    expect(pipe).toBeTruthy();
  });
});
